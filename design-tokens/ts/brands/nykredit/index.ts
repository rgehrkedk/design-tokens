
import baseTokens from './baseTokens.json';
import lightMode from './light.json';
import darkMode from './dark.json';
import componentTokens from './components.json';

export const nykreditTokens = {
  base: baseTokens,
  themes: {
    light: lightMode,
    dark: darkMode,
  },
  components: componentTokens
};
